---
phase: 05-extract-view-components-final-refactor
plan: 05
type: execute
wave: 5
depends_on: ["05-04"]
files_modified:
  - main.py
  - CLAUDE.md
  - CHANGELOG.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "All four tabs render correctly and identically to before extraction"
    - "All UI controls work (buttons, inputs, dropdowns, sliders)"
    - "Controllers can access all widgets through view.get_widgets()"
    - "Full video processing pipeline works end-to-end"
    - "Config save/load works correctly"
    - "main.py is under 500 lines"
  artifacts:
    - path: "main.py"
      provides: "Minimal main.py with view assembly only"
      max_lines: 500
    - path: "CLAUDE.md"
      provides: "Updated documentation with Phase 5 info"
      contains: "Phase 5"
    - path: "CHANGELOG.md"
      provides: "Updated changelog with Phase 5 completion"
      contains: "Phase 5"
  key_links:
    - from: "main.py"
      to: "src/views/*"
      via: "All four view imports and instantiation"
      pattern: "from src.views import.*LogView.*DubView.*SourceView.*AdjustView"
---

<objective>
Perform final integration testing, verify all functionality works after view extraction, update documentation, and confirm Phase 5 success criteria.

Purpose: Ensure the complete view extraction didn't break any functionality and document the final refactored architecture.

Output: Fully tested application with all views extracted, main.py under 500 lines, and updated documentation.
</objective>

<execution_context>
@C:/Users/xhiep/Downloads/dubbing-extractor/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/xhiep/Downloads/dubbing-extractor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/xhiep/Downloads/dubbing-extractor/.planning/PROJECT.md
@C:/Users/xhiep/Downloads/dubbing-extractor/.planning/ROADMAP.md
@C:/Users/xhiep/Downloads/dubbing-extractor/.planning/STATE.md
@C:/Users/xhiep/Downloads/dubbing-extractor/.planning/phases/05-extract-view-components-final-refactor/05-RESEARCH.md
@C:/Users/xhiep/Downloads/dubbing-extractor/.planning/phases/05-extract-view-components-final-refactor/05-04-SUMMARY.md
@C:/Users/xhiep/Downloads/dubbing-extractor/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify main.py line count and structure</name>
  <files>main.py</files>
  <read_first>
    - main.py (verify final structure after all extractions)
  </read_first>
  <action>
Count lines in main.py and verify it meets the target of under 500 lines.

**Verification steps:**
1. Count total lines: `wc -l main.py`
2. Verify main.py structure contains only:
   - Imports (stdlib, third-party, local)
   - Helper functions (paste_clipboard, browse_file, etc.)
   - launch_gui() function with:
     - Window setup (SECTION 1)
     - Configuration & state management (SECTION 2)
     - Notebook creation (SECTION 3)
     - View instantiation (SECTIONS 4-7 now minimal)
     - Controller initialization (SECTION 8)
     - Application start (SECTION 9)

**Expected line count breakdown:**
- Imports: ~60 lines
- Helper functions: ~150 lines
- launch_gui() setup: ~100 lines
- View instantiation: ~80 lines (4 views × ~20 lines each)
- Controller wiring: ~50 lines
- Application start: ~10 lines
- **Total: ~450 lines** (target: < 500)

**If over 500 lines:** Identify remaining extractable code (helper functions that could move to src/utils/).

No code changes expected - this is verification only.
  </action>
  <verify>
    <automated>wc -l main.py && python -m py_compile main.py</automated>
  </verify>
  <acceptance_criteria>
    - main.py line count is under 500 lines
    - main.py compiles without syntax errors
    - main.py contains only: imports, helper functions, launch_gui(), view assembly, controller wiring
    - No inline tab UI code remains (all extracted to views)
    - All four views are imported and instantiated
  </acceptance_criteria>
  <done>main.py verified under 500 lines with clean structure</done>
</task>

<task type="auto">
  <name>Task 2: Run automated verification checks</name>
  <files>main.py, src/views/*.py</files>
  <read_first>
    - main.py (verify imports and structure)
    - src/views/__init__.py (verify all exports)
    - src/views/log_view.py
    - src/views/dub_view.py
    - src/views/source_view.py
    - src/views/adjust_view.py
  </read_first>
  <action>
Run automated checks to verify code quality and imports.

**Verification commands:**
1. Syntax check all files:
   ```bash
   python -m py_compile main.py
   python -m py_compile src/views/log_view.py
   python -m py_compile src/views/dub_view.py
   python -m py_compile src/views/source_view.py
   python -m py_compile src/views/adjust_view.py
   ```

2. Import verification:
   ```bash
   python -c "import sys; sys.path.insert(0, '.'); from src.views import LogView, DubView, SourceView, AdjustView; print('All views import successfully')"
   ```

3. Check for unused imports in main.py:
   ```bash
   # Visual inspection - ensure no unused view-related imports
   grep "^from\|^import" main.py
   ```

4. Verify view exports:
   ```bash
   python -c "from src.views import __all__; print('Exported views:', __all__)"
   ```

**Expected results:**
- All files compile without errors
- All views import successfully
- __all__ exports: ["LogView", "DubView", "SourceView", "AdjustView"]
- No unused imports in main.py

No code changes expected unless verification reveals issues.
  </action>
  <verify>
    <automated>python -m py_compile main.py src/views/*.py && python -c "from src.views import LogView, DubView, SourceView, AdjustView"</automated>
  </verify>
  <acceptance_criteria>
    - All Python files compile without syntax errors
    - All four views can be imported successfully
    - src/views/__all__ contains all four view classes
    - No import errors or circular dependencies
    - No unused imports in main.py
  </acceptance_criteria>
  <done>Automated verification checks passed</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete view extraction for all four tabs (Log, Dub, Source, Adjust). All tab UI code moved from main.py to separate view classes in src/views/. main.py reduced from ~2,527 lines to under 500 lines.

**Changes made:**
- Created src/views/ module with 4 view classes
- LogView: Log tab UI (~50 lines)
- DubView: Dub tab UI (~250 lines)
- SourceView: Source tab UI (~500 lines)
- AdjustView: Adjust tab UI with scroll setup (~900 lines)
- main.py: Now only contains window setup, view assembly, controller wiring

**Architecture after Phase 5:**
```
main.py (~450 lines)
  ↓
src/views/ (UI structure)
  ├── log_view.py
  ├── dub_view.py
  ├── source_view.py
  └── adjust_view.py
  ↓
src/controllers/ (event handlers)
  ├── source_controller.py
  ├── subtitle_controller.py
  ├── tts_controller.py
  └── app_controller.py
  ↓
src/modules/ (business logic)
  └── workflow.py, tts/, downloader/, etc.
```
  </what-built>
  <how-to-verify>
**Full manual testing checklist:**

1. **Launch application:**
   ```bash
   cd C:\Users\xhiep\Downloads\dubbing-extractor
   scripts\run.bat
   ```
   - Verify app launches without errors
   - Verify window renders correctly

2. **Test all four tabs render:**
   - Click "Nguồn" tab → verify source input, buttons, step controls visible
   - Click "Điều Chỉnh" tab → verify subtitle controls, preview canvas visible, scrollbar works
   - Click "Lồng Tiếng" tab → verify TTS controls, voice dropdown visible
   - Click "Nhật Ký" tab → verify log area visible

3. **Test Source tab functionality:**
   - Click "Dán Link" button → verify clipboard paste works
   - Click "Chọn File" button → verify file dialog opens
   - Type in source input → verify text appears
   - Press Enter in source input → verify preview loads (if valid source)

4. **Test Adjust tab functionality:**
   - Scroll with mouse wheel → verify canvas scrolls smoothly
   - Drag scrollbar → verify canvas scrolls
   - Change cover mode (blur/blackbar/none) → verify radio buttons work
   - Change preset dropdown → verify preset selection works
   - Adjust blur radius slider → verify slider moves
   - Adjust blackbar height slider → verify slider moves

5. **Test Dub tab functionality:**
   - Change TTS mode → verify radio buttons work
   - Change voice dropdown → verify voice selection works
   - Toggle quick dub checkbox → verify checkbox works
   - Click "Chọn File" for reference audio → verify file dialog opens

6. **Test Log tab functionality:**
   - Click "Xóa Nhật Ký" button → verify log clears
   - Trigger any action that logs → verify log messages appear

7. **Test config persistence:**
   - Change some settings (source input, cover mode, voice)
   - Close application
   - Reopen application
   - Verify settings are restored from config.json

8. **Test full pipeline (if time permits):**
   - Load a video source (local file or URL)
   - Click "Bắt Đầu Xử Lý"
   - Verify processing runs without errors
   - Verify output files are created

**Expected results:**
- All tabs render identically to before Phase 5
- All buttons, inputs, dropdowns, sliders work
- Scrolling works in Adjust tab
- Config save/load works
- No visual regressions
- No functional regressions
  </how-to-verify>
  <resume-signal>
Type "approved" if all tests pass, or describe any issues found.

If issues found, describe:
- Which tab/control has the issue
- What the expected behavior is
- What the actual behavior is
- Any error messages in console or log
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md with Phase 5 information</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (current documentation)
    - main.py (verify final line count)
  </read_first>
  <action>
Update CLAUDE.md to reflect Phase 5 completion and new architecture.

**Updates to make:**

1. Update line count in "Cấu trúc chính" section:
   ```markdown
   ## Cấu trúc chính
   - `main.py` — entry point + window setup (~450 dòng, organized with view assembly)
   - `src/views/` — tab UI components (LogView, DubView, SourceView, AdjustView)
   - `src/controllers/` — event handlers (Phase 2 extraction)
   - `src/modules/workflow.py` — pipeline xử lý video (~567 dòng)
   - `src/modules/tts/` — VieNeu-TTS lồng tiếng tiếng Việt
   - `src/components/` — custom UI components (Card, Button, Input, TextArea...)
   - `src/utils/ui_helpers.py` — pure UI geometry helper functions (Phase 3 extraction)
   - `src/utils/logger.py` — structured logging with rotation (Phase 4)
   ```

2. Add Phase 5 section after Phase 4:
   ```markdown
   ## View Extraction (Phase 5)

   **Views (src/views/):**
   - Extracted all tab UI code from main.py to separate view classes
   - Each view builds and returns a configured tk.Frame
   - Views are pure UI - no business logic

   **Architecture:**
   - main.py: Window setup + view assembly (~450 lines, 82% reduction from original 2,527)
   - Views: Tab UI structure (LogView, DubView, SourceView, AdjustView)
   - Controllers: Event handlers (thin wrappers)
   - Modules: Business logic (workflow, TTS, downloader)

   **Benefits:**
   - Complete MVC separation
   - Each tab is independently maintainable
   - main.py is now minimal and focused
   ```

3. Update backup reference if needed (keep existing backup path).

**Write the updated CLAUDE.md with these changes.**
  </action>
  <verify>
    <automated>grep -q "Phase 5" CLAUDE.md && grep -q "src/views/" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains Phase 5 section
    - Line count for main.py updated to ~450 lines
    - src/views/ mentioned in structure section
    - Architecture description includes view layer
    - All other sections remain accurate
  </acceptance_criteria>
  <done>CLAUDE.md updated with Phase 5 information</done>
</task>

<task type="auto">
  <name>Task 4: Update CHANGELOG.md with Phase 5 completion</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (current changelog)
    - .planning/ROADMAP.md (Phase 5 description)
  </read_first>
  <action>
Update CHANGELOG.md to document Phase 5 completion.

**Add Phase 5 entry:**

```markdown
## Phase 5: Extract View Components (Final Refactor) - 2026-04-24

**Goal:** Tách 4 tabs UI ra thành view files riêng, giảm main.py xuống ~400 dòng

**Changes:**
- Created `src/views/` module with 4 view classes
- Extracted Log tab to `LogView` (~50 lines)
- Extracted Dub tab to `DubView` (~250 lines)
- Extracted Source tab to `SourceView` (~500 lines)
- Extracted Adjust tab to `AdjustView` (~900 lines, includes complex scroll setup)
- Reduced main.py from 2,527 lines to ~450 lines (82% reduction)

**Architecture:**
- Complete MVC separation achieved
- Views: Pure UI structure (no business logic)
- Controllers: Event handlers (thin wrappers from Phase 2)
- Modules: Business logic (unchanged)

**Files Modified:**
- Created: `src/views/__init__.py`, `src/views/log_view.py`, `src/views/dub_view.py`, `src/views/source_view.py`, `src/views/adjust_view.py`
- Modified: `main.py` (reduced by ~2,000 lines)
- Updated: `CLAUDE.md` (Phase 5 documentation)

**Testing:**
- All four tabs render identically
- All UI controls functional
- Scroll behavior works in Adjust tab
- Config save/load works
- Full pipeline tested and working

**Metrics:**
- main.py: 2,527 → ~450 lines (82% reduction)
- Total lines extracted: ~2,000 lines to src/views/
- Code organization: Complete MVC separation
```

**Write the updated CHANGELOG.md with this entry at the top (after any existing Phase 5 entry, or as a new entry).**
  </action>
  <verify>
    <automated>grep -q "Phase 5" CHANGELOG.md && grep -q "src/views/" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - CHANGELOG.md contains Phase 5 entry
    - Entry includes date (2026-04-24)
    - Entry lists all view files created
    - Entry documents line count reduction (2,527 → ~450)
    - Entry mentions complete MVC separation
    - Entry lists files modified
  </acceptance_criteria>
  <done>CHANGELOG.md updated with Phase 5 completion</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Phase 5 is pure code refactoring - no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | N/A | Integration testing | accept | No security impact - verification only |
</threat_model>

<verification>
**Automated verification:**
- Line count check: `wc -l main.py` (should be < 500)
- Syntax check: `python -m py_compile main.py src/views/*.py`
- Import check: `python -c "from src.views import LogView, DubView, SourceView, AdjustView"`

**Manual verification (checkpoint):**
- Full application testing (all tabs, all controls)
- Config persistence testing
- Visual regression testing
- Full pipeline testing (optional but recommended)

**Documentation verification:**
- CLAUDE.md updated with Phase 5 info
- CHANGELOG.md updated with Phase 5 entry
</verification>

<success_criteria>
- main.py under 500 lines (target: ~450 lines)
- All four views extracted and working
- All tabs render identically to before Phase 5
- All UI controls functional
- Config save/load works
- No syntax or import errors
- CLAUDE.md updated
- CHANGELOG.md updated
- User confirms all functionality works (checkpoint approval)
</success_criteria>

<output>
After completion, create `.planning/phases/05-extract-view-components-final-refactor/05-05-SUMMARY.md`
</output>
