---
phase: 05-extract-view-components-final-refactor
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/views/__init__.py
  - src/views/source_view.py
  - main.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Source tab renders correctly with video input and processing controls"
    - "Step-by-step buttons render and are accessible"
    - "Source input field works with paste and browse functions"
    - "All source tab widgets are accessible to SourceController"
  artifacts:
    - path: "src/views/source_view.py"
      provides: "Source tab view class"
      min_lines: 450
      exports: ["SourceView"]
    - path: "src/views/__init__.py"
      provides: "Updated exports including SourceView"
      contains: "from .source_view import SourceView"
    - path: "main.py"
      provides: "Updated to use SourceView"
      contains: "from src.views import LogView, DubView, SourceView"
  key_links:
    - from: "main.py"
      to: "src/views/source_view.py"
      via: "SourceView import and instantiation"
      pattern: "SourceView\\(notebook"
    - from: "src/views/source_view.py"
      to: "src/components"
      via: "Component imports (Button, Input, Card, etc.)"
      pattern: "from.*components"
---

<objective>
Extract the Source tab (video input and processing controls) to SourceView class, reducing main.py by ~552 lines.

Purpose: Extract the source tab including step-by-step UI controls, establishing the pattern for complex tab extraction before tackling the most complex Adjust tab.

Output: Working SourceView class that renders the source tab identically to current implementation.
</objective>

<execution_context>
@C:/Users/xhiep/Downloads/dubbing-extractor/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/xhiep/Downloads/dubbing-extractor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/xhiep/Downloads/dubbing-extractor/.planning/PROJECT.md
@C:/Users/xhiep/Downloads/dubbing-extractor/.planning/ROADMAP.md
@C:/Users/xhiep/Downloads/dubbing-extractor/.planning/STATE.md
@C:/Users/xhiep/Downloads/dubbing-extractor/.planning/phases/05-extract-view-components-final-refactor/05-RESEARCH.md
@C:/Users/xhiep/Downloads/dubbing-extractor/.planning/phases/05-extract-view-components-final-refactor/05-02-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts from existing codebase -->

From main.py (state variables used in Source tab):
```python
source_state: use_tk_state("string", ...)  # Video source input
whisper_model_state: use_tk_state("string", ...)  # Whisper model selection
pipeline_state: Dict  # Step-by-step processing state
```

From main.py (callbacks used in Source tab):
```python
def paste_clipboard() -> None
def browse_file() -> None
def load_source_preview(force: bool = False) -> None
def log(message: str) -> None
```

From src/modules/workflow:
```python
def step1_prepare(...) -> Dict
def step2_transcribe(...) -> Dict
def step3_translate(...) -> Dict
def step4_cover(...) -> Dict
def step5_export(...) -> Dict
def step6_burn(...) -> Dict
def step7_dub(...) -> Dict
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract Source tab to SourceView class</name>
  <files>src/views/source_view.py, src/views/__init__.py, main.py</files>
  <read_first>
    - main.py (lines 376-927 - current Source tab implementation, SECTION 4)
    - src/views/dub_view.py (to follow same pattern)
    - src/components/ui/__init__.py (available UI components)
    - src/modules/workflow.py (step functions used in source tab)
  </read_first>
  <action>
Create src/views/source_view.py with SourceView class that builds the source tab UI.

**Implementation strategy:**
1. Extract lines 376-927 from main.py (SECTION 4: Source Tab)
2. Convert to SourceView class following DubView pattern
3. Pass state variables and callbacks via constructor
4. Store widget references for SourceController access
5. Include step-by-step UI section (lines 408-577 approximately)

**SourceView class structure:**

```python
"""Source tab view - video input and processing controls."""
import tkinter as tk
from typing import Dict, Any, Callable

from ..components.ui import Button, Input, Label, Checkbox
from ..components.layout import Card, Row, Section
from ..components.theme import T


class SourceView:
    """Source tab view - video source input, model selection, and step-by-step controls."""
    
    def __init__(self, 
                 parent: tk.Widget,
                 state: Dict[str, Any],
                 callbacks: Dict[str, Callable]):
        """Initialize source view.
        
        Args:
            parent: Parent widget (notebook)
            state: Dictionary of tk state variables
                - source_state: StringVar for video source input
                - whisper_model_state: StringVar for Whisper model selection
                - pipeline_state: Dict for step-by-step processing state
                - preview_guard: Dict for preview state management
            callbacks: Dictionary of callback functions
                - paste_clipboard: Function to paste from clipboard
                - browse_file: Function to open file dialog
                - load_source_preview: Function to load video preview
                - log: Function to log messages
                - run_step: Function to run individual processing steps
        """
        self.parent = parent
        self.state = state
        self.callbacks = callbacks
        self.widgets = {}
    
    def build(self) -> tk.Frame:
        """Build and return the source tab frame.
        
        Returns:
            Configured tk.Frame ready to add to notebook
        """
        # Create tab frame
        frame = tk.Frame(self.parent, padx=16, pady=16, bg=T.BG_WHITE)
        
        # Extract the exact UI code from lines 376-927
        # This includes:
        # - Video source card (input, paste/browse buttons)
        # - Source hint label
        # - Whisper model selection card
        # - Step-by-step processing card (7 step buttons)
        # - Preview section (if exists)
        
        # Keep all Card, Button, Input, Label creation
        # Keep all layout logic (pack, grid)
        # Replace direct state variable access with self.state['key']
        # Replace callback function calls with self.callbacks['key']()
        
        # Store widget references in self.widgets for controller access
        # self.widgets['source_input'] = source_input
        # self.widgets['step_buttons'] = {1: btn1, 2: btn2, ...}
        # self.widgets['whisper_model_dropdown'] = whisper_model_dropdown
        # etc.
        
        return frame
    
    def get_widgets(self) -> Dict[str, Any]:
        """Get widget references for controller access.
        
        Returns:
            Dictionary of widget references
        """
        return self.widgets
```

**Update src/views/__init__.py:**
Add SourceView to exports:
```python
from .log_view import LogView
from .dub_view import DubView
from .source_view import SourceView

__all__ = ["LogView", "DubView", "SourceView"]
```

**Update main.py:**
1. Update import (line ~55): `from src.views import LogView, DubView, SourceView`
2. Replace SECTION 4 (lines 376-927) with:
```python
    # ═══════════════════════════════════════════════════════════
    # SECTION 4: Source Tab - Video Input & Processing
    # ═══════════════════════════════════════════════════════════
    source_view_state = {
        'source_state': source_state,
        'whisper_model_state': whisper_model_state,
        'pipeline_state': pipeline_state,
        'preview_guard': preview_guard,
    }
    source_view_callbacks = {
        'paste_clipboard': paste_clipboard,
        'browse_file': browse_file,
        'load_source_preview': load_source_preview,
        'log': log,
    }
    source_view = SourceView(notebook, source_view_state, source_view_callbacks)
    source_tab = source_view.build()
    source_widgets = source_view.get_widgets()
```
3. Keep notebook.add(source_tab, ...) line unchanged (line 319)

**Critical extraction notes:**
- Lines 376-927 contain the complete source tab UI (~552 lines)
- Preserve all widget creation, styling, and layout logic exactly
- Step-by-step UI section (lines 408-577) must be included in SourceView
- Replace state variable access: `source_state.get()` → `self.state['source_state'].get()`
- Replace callback calls: `paste_clipboard()` → `self.callbacks['paste_clipboard']()`
- Store all widgets that SourceController needs in self.widgets dict
- Preserve event bindings: `source_input.widget.bind("<Return>", ...)`
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, '.'); from src.views import SourceView; print('SourceView import successful')" && python -m py_compile src/views/source_view.py</automated>
  </verify>
  <acceptance_criteria>
    - File src/views/source_view.py exists with SourceView class
    - SourceView.__init__ has type hints for parent, state, callbacks
    - SourceView.build() returns tk.Frame
    - SourceView.build() creates video source input card
    - SourceView.build() creates Whisper model selection card
    - SourceView.build() creates step-by-step processing card with 7 buttons
    - SourceView.get_widgets() returns dictionary of widget references
    - src/views/__init__.py exports SourceView
    - main.py imports SourceView
    - main.py SECTION 4 uses SourceView instead of inline code
    - main.py line count reduced by ~540 lines
    - Source tab renders identically
    - All source controls work (input, paste, browse, model selection)
    - Step-by-step buttons render correctly
  </acceptance_criteria>
  <done>SourceView extracted, main.py updated, source tab renders correctly</done>
</task>

<task type="auto">
  <name>Task 2: Verify source tab functionality and SourceController integration</name>
  <files>main.py, src/views/source_view.py</files>
  <read_first>
    - main.py (verify SourceView integration and SourceController wiring)
    - src/views/source_view.py (verify implementation)
    - src/controllers/source_controller.py (verify controller can access widgets)
  </read_first>
  <action>
Run syntax check and verify SourceController can still access source tab widgets.

**Verification steps:**
1. Python syntax check: `python -m py_compile main.py src/views/source_view.py`
2. Import check: `python -c "from src.views import SourceView"`
3. Check that source_widgets dict is populated after SourceView.build()
4. Verify SourceController initialization still works with new widget structure
5. Check that pipeline_state and preview_guard are accessible
6. Verify event bindings work (Return key on source input, FocusOut event)

**Check for common issues:**
- Missing widget references in source_widgets dict
- Broken state variable bindings (StringVar for source, whisper model)
- Missing callback function references
- Broken event bindings (<Return>, <FocusOut>)
- Step buttons not accessible to controller

No code changes needed unless verification reveals issues.
  </action>
  <verify>
    <automated>python -m py_compile main.py && python -m py_compile src/views/source_view.py && python -c "import sys; sys.path.insert(0, '.'); from src.views import LogView, DubView, SourceView"</automated>
  </verify>
  <acceptance_criteria>
    - main.py compiles without syntax errors
    - src/views/source_view.py compiles without syntax errors
    - SourceView can be imported successfully
    - No import errors when running main.py
    - source_widgets dictionary contains all expected widget references
    - SourceController can access widgets via source_widgets
    - State variables (source_state, whisper_model_state, pipeline_state) are bound correctly
    - Event bindings work (Return key, FocusOut)
    - Step buttons are accessible and functional
  </acceptance_criteria>
  <done>Source tab extraction verified, SourceController integration confirmed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Phase 5 is pure code refactoring - no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | SourceView.build() | accept | View extraction doesn't change security posture - all input validation remains in workflow.py and downloader modules |
</threat_model>

<verification>
**Syntax verification:**
- Python compile check on main.py and source_view.py
- Import verification for SourceView

**Visual verification (manual):**
- Launch app: `scripts\run.bat`
- Navigate to "Nguồn" tab
- Verify video source input field renders correctly
- Verify paste and browse buttons work
- Verify Whisper model dropdown works
- Verify step-by-step buttons render (7 buttons)
- Test source input with Return key
- Test paste clipboard functionality
- Test browse file dialog
</verification>

<success_criteria>
- SourceView class implemented following DubView pattern
- Source tab renders identically to before extraction
- main.py reduced by ~540 lines
- No syntax or import errors
- SourceController can access all source tab widgets
- All source controls functional (input, paste, browse, model selection)
- Step-by-step buttons accessible and functional
</success_criteria>

<output>
After completion, create `.planning/phases/05-extract-view-components-final-refactor/05-03-SUMMARY.md`
</output>
