---
phase: 03-split-main-py-further-higher-risk
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - main.py
  - C:/Users/xhiep/Downloads/dubbing-extractor/CLAUDE.md
autonomous: false
requirements: [NFR1, NFR6, FR1, FR2]

must_haves:
  truths:
    - All features work identically to before refactoring
    - UI layout and behavior unchanged
    - Config save/load works correctly
    - Full pipeline processes video successfully
  artifacts:
    - path: main.py
      provides: Final cleaned and organized code
      contains: "def launch_gui():"
    - path: CLAUDE.md
      provides: Updated documentation with correct line counts
      contains: "main.py — entry point"
  key_links:
    - from: User
      to: Application
      via: Manual testing
      pattern: "All features verified working"

---

<objective>
Final cleanup and comprehensive testing to verify Phase 3 refactoring is complete and all features work correctly.

Purpose: Ensure the refactoring achieved its goals (improved organization, reduced complexity) without breaking any functionality.

Output: Verified working application with improved code structure, updated documentation.
</objective>

<execution_context>
@C:/Users/xhiep/Downloads/dubbing-extractor/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/xhiep/Downloads/dubbing-extractor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-split-main-py-further-higher-risk/03-RESEARCH.md
@.planning/phases/03-split-main-py-further-higher-risk/03-01-PLAN.md
@.planning/phases/03-split-main-py-further-higher-risk/03-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md documentation</name>
  <files>C:/Users/xhiep/Downloads/dubbing-extractor/CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (to see current documentation)
    - main.py (to get accurate line count)
  </read_first>
  <action>
Update CLAUDE.md to reflect Phase 3 changes:

1. **Update line count for main.py:**
   - Current: Run `wc -l main.py` to get exact count
   - Update the line that says "main.py — entry point + toàn bộ GUI (Tkinter, ~2481 dòng)" or similar
   - New value should be approximately 2,517 lines (after Plan 01 and 02)

2. **Update structure description:**
   - Add note about code organization improvements
   - Mention new ui_helpers.py module

Example update:
```markdown
## Cấu trúc chính
- `main.py` — entry point + toàn bộ GUI (Tkinter, ~2,517 dòng, organized with section markers)
- `src/modules/workflow.py` — pipeline xử lý video (download → transcribe → translate → render, ~567 dòng)
- `src/modules/tts/` — VieNeu-TTS lồng tiếng tiếng Việt
- `src/components/` — custom UI components (Card, Button, Input, TextArea...)
- `src/controllers/` — event handlers (Phase 2 extraction)
- `src/utils/ui_helpers.py` — pure UI geometry helper functions (Phase 3 extraction)
```

3. **Keep all other sections unchanged:**
   - Khởi động Claude
   - Chạy app
   - ffmpeg
   - Pipeline workflow
   - Tính năng hiện tại
   - Lưu ý code

Only update the "Cấu trúc chính" section with accurate line counts and new module mention.
  </action>
  <acceptance_criteria>
    - CLAUDE.md contains updated line count for main.py (approximately 2,517)
    - CLAUDE.md mentions src/utils/ui_helpers.py module
    - CLAUDE.md mentions "organized with section markers" or similar
    - All other sections remain unchanged
    - grep "main.py.*~[0-9]+ dòng" CLAUDE.md shows updated count
    - grep "ui_helpers" CLAUDE.md returns match
  </acceptance_criteria>
  <verify>
    <automated>grep "main.py" CLAUDE.md | grep -o "[0-9,]\\+ dòng"</automated>
  </verify>
  <done>CLAUDE.md updated with accurate line counts and structure description</done>
</task>

<task type="auto">
  <name>Task 2: Run automated verification checks</name>
  <files>main.py, src/utils/ui_helpers.py</files>
  <read_first>
    - main.py (to verify final state)
    - src/utils/ui_helpers.py (to verify extraction)
  </read_first>
  <action>
Run comprehensive automated checks to verify Phase 3 is complete:

1. **Syntax checks:**
   ```bash
   python -m py_compile main.py
   python -m py_compile src/utils/ui_helpers.py
   ```

2. **Import checks:**
   ```bash
   python -c "from src.utils.ui_helpers import expand_band_from_center, shift_band; print('ui_helpers OK')"
   python -c "import main; print('main.py OK')"
   ```

3. **Function extraction verification:**
   ```bash
   # Verify old function names removed
   grep -c "_expand_band_from_center" main.py  # Should be 0
   grep -c "_shift_band" main.py  # Should be 0
   
   # Verify new function names present
   grep -c "expand_band_from_center" main.py  # Should be > 0
   grep -c "shift_band" main.py  # Should be > 0
   ```

4. **Organization verification:**
   ```bash
   # Verify import categories
   grep -c "# ── Standard Library ──" main.py  # Should be 1
   grep -c "# ── Third-Party ──" main.py  # Should be 1
   grep -c "# ── Local Modules ──" main.py  # Should be 1
   
   # Verify section markers
   grep -c "# ═══" main.py  # Should be 27 (9 sections × 3 lines)
   grep -c "SECTION [0-9]:" main.py  # Should be 9
   ```

5. **Line count verification:**
   ```bash
   wc -l main.py  # Should be approximately 2,517 lines
   wc -l src/utils/ui_helpers.py  # Should be approximately 50-60 lines
   ```

6. **Document results:**
   - All checks passed: Phase 3 automated verification complete
   - Ready for manual testing
  </action>
  <acceptance_criteria>
    - All py_compile commands exit 0
    - All import tests print "OK"
    - grep "_expand_band_from_center" main.py returns 0 matches
    - grep "_shift_band" main.py returns 0 matches
    - grep "expand_band_from_center" main.py returns > 0 matches
    - grep "# ── Standard Library ──" main.py returns 1 match
    - grep "# ═══" main.py returns 27 matches
    - grep "SECTION [0-9]:" main.py returns 9 matches
    - main.py is approximately 2,517 lines (±20 lines acceptable)
    - ui_helpers.py is approximately 50-60 lines
  </acceptance_criteria>
  <verify>
    <automated>python -m py_compile main.py && python -m py_compile src/utils/ui_helpers.py && grep -c "SECTION [0-9]:" main.py</automated>
  </verify>
  <done>All automated checks passed: syntax valid, imports work, organization correct, line counts as expected</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual testing verification</name>
  <what-built>
Phase 3 refactoring complete:
- Extracted 2 pure helper functions to src/utils/ui_helpers.py
- Organized imports into 3 clear categories
- Added 9 major section markers to launch_gui()
- Updated CLAUDE.md documentation
- main.py reduced from 2,516 to ~2,517 lines (net +1 due to section markers, but -26 from extraction + 27 from markers)

All automated checks passed. Now need manual verification that all features work correctly.
  </what-built>
  <how-to-verify>
**Manual Testing Checklist** (from REQUIREMENTS.md FR1):

1. **Launch application:**
   ```bash
   cd C:/Users/xhiep/Downloads/dubbing-extractor
   scripts\run.bat
   ```
   - ✅ Application window opens without errors
   - ✅ All tabs visible (Source, Adjust, Dub, Log)
   - ✅ UI layout looks correct (no visual changes)

2. **Test Source tab:**
   - ✅ Paste URL or browse for local file
   - ✅ Source input field works
   - ✅ Preview button works (if applicable)

3. **Test Adjust tab:**
   - ✅ Subtitle preset dropdown works
   - ✅ Cover mode selection works (blur/blackbar/none)
   - ✅ All parameter sliders work
   - ✅ Preview subtitle works

4. **Test Dub tab:**
   - ✅ TTS mode selection works
   - ✅ Voice selection works
   - ✅ Preview TTS works (if applicable)

5. **Test processing (quick test):**
   - ✅ Start processing with a short video or test file
   - ✅ Progress shows in log tab
   - ✅ No errors during processing
   - ✅ Output files created

6. **Test config persistence:**
   - ✅ Change some settings
   - ✅ Close application
   - ✅ Reopen application
   - ✅ Settings are preserved

**Expected result:** All features work identically to before Phase 3 refactoring.

**If any issues found:** Note them in your response with details (which feature, what error, steps to reproduce).
  </how-to-verify>
  <resume-signal>
Type one of:
- "approved" - All tests passed, no issues found
- "approved with notes: [details]" - Tests passed but minor observations
- "issues found: [details]" - Problems discovered, need fixes

Include any observations about performance, UI behavior, or errors encountered.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Testing and documentation only - no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | CLAUDE.md | accept | Documentation file contains no sensitive information |

No new security concerns - testing and documentation updates do not change security posture.
</threat_model>

<verification>
After all tasks complete:

1. **Documentation Updated**: CLAUDE.md has accurate line counts
2. **Automated Checks Passed**: All syntax, import, and organization checks pass
3. **Manual Testing Complete**: User verifies all features work correctly
4. **No Regressions**: All functionality preserved from before Phase 3
5. **Goals Achieved**: Code better organized, easier to navigate
</verification>

<success_criteria>
- ✅ CLAUDE.md updated with accurate information
- ✅ All automated checks pass
- ✅ User confirms all features work correctly
- ✅ No regressions introduced
- ✅ Phase 3 goals achieved:
  - Pure functions extracted
  - Imports organized
  - Section markers added
  - Code navigation improved
</success_criteria>

<output>
After completion, create `.planning/phases/03-split-main-py-further-higher-risk/03-03-SUMMARY.md`
</output>
