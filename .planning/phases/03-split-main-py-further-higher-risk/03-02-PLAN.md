---
phase: 03-split-main-py-further-higher-risk
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - main.py
autonomous: true
requirements: [NFR1, NFR6]

must_haves:
  truths:
    - Imports organized into clear categories (stdlib, third-party, local)
    - Section markers added throughout launch_gui() for navigation
    - Code structure is clear and maintainable
  artifacts:
    - path: main.py
      provides: Organized imports and clear section structure
      contains: "# ── Standard Library ──"
  key_links:
    - from: main.py imports
      to: organized sections
      via: category comments
      pattern: "# ── (Standard Library|Third-Party|Local Modules) ──"

---

<objective>
Reorganize main.py with clear import categories and comprehensive section markers to improve code navigation and maintainability.

Purpose: Make main.py easier to navigate by organizing imports logically and adding clear section markers throughout the 2,300+ line launch_gui() function.

Output: main.py with organized imports and 8-10 major section markers for easy navigation.
</objective>

<execution_context>
@C:/Users/xhiep/Downloads/dubbing-extractor/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/xhiep/Downloads/dubbing-extractor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-split-main-py-further-higher-risk/03-RESEARCH.md
@.planning/phases/03-split-main-py-further-higher-risk/03-01-PLAN.md
</context>

<interfaces>
<!-- Existing section marker style from main.py -->

Current style (lines 167-227):
```python
    # ── Notebook (tabs) ──────────────────────────────────────────────────────
    # ── Scrollbar ────────────────────────────────────────────────────────────
    # ── Combobox ─────────────────────────────────────────────────────────────
```

Target style for major sections (from RESEARCH.md):
```python
    # ═══════════════════════════════════════════════════════════
    # SECTION 1: Window Setup
    # ═══════════════════════════════════════════════════════════
```

Use double-line (═══) for major sections, single-line (───) for subsections.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Organize imports by category</name>
  <files>main.py</files>
  <read_first>
    - main.py (lines 1-60 contain all imports)
  </read_first>
  <action>
Reorganize imports in main.py (lines 7-52) into three clear categories:

**Current state:** Imports are somewhat mixed (stdlib, third-party, local all together)

**Target state:** Three sections with clear separators:

```python
#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""Dubbing Extractor v2 - Main Entry Point.

Modular architecture with component-based UI.
"""

# ── Standard Library ──────────────────────────────────────────
import json
import os
import sys
from datetime import datetime
from pathlib import Path

# ── Third-Party ───────────────────────────────────────────────
import tkinter as tk
from tkinter import ttk
import winsound

# ── Local Modules ─────────────────────────────────────────────
# Add src to path
HERE = Path(__file__).parent
sys.path.insert(0, str(HERE))

from src.utils.suppress_warnings import install_stderr_filter
install_stderr_filter()

from src.config import config, load_app_config, save_app_config
from src.modules.workflow import (
    process_video,
    step1_prepare,
    step2_transcribe,
    step3_translate,
    step4_cover,
    step5_export,
    step6_burn,
    step7_dub,
)
from src.modules.downloader.ytdlp_wrapper import check_source_preconditions, fetch_preview_info
from src.modules.video_processing.ffmpeg_wrapper import ffmpeg_cmd, get_dims, probe_duration
from src.modules.video_processing.subtitle_burner import compute_subtitle_layout
from src.modules.tts import (
    get_vieneu_error,
    is_vieneu_available,
    list_preset_voices,
    release_tts_resources,
    render_dubbed_outputs,
    synthesize_speech,
)
from src.components.ui import Button, Input, Label, TextArea, Checkbox
from src.components.layout import Card, Section, Row, Column
from src.components.theme import T
from src.components.hooks import use_tk_state
from src.controllers import SourceController, SubtitleController, TtsController, AppController
from src.utils.file_utils import is_local_file
from src.utils.runtime_env import ensure_local_runtime_env
from src.utils.ui_helpers import expand_band_from_center, shift_band

ensure_local_runtime_env()
```

**Order within each category:**
- Standard library: alphabetical by module name
- Third-party: alphabetical by module name
- Local: grouped by purpose (utils, config, modules, components, controllers)

**Keep:** subprocess import is used later in the file, verify it's included.
  </action>
  <acceptance_criteria>
    - main.py contains "# ── Standard Library ──" comment
    - main.py contains "# ── Third-Party ──" comment
    - main.py contains "# ── Local Modules ──" comment
    - Standard library imports (json, os, sys, datetime, pathlib) appear before third-party
    - Third-party imports (tkinter, winsound) appear before local modules
    - Local imports grouped logically (utils, config, modules, components, controllers)
    - All original imports still present (no imports lost)
    - grep "^import \|^from " main.py | head -60 shows organized structure
  </acceptance_criteria>
  <verify>
    <automated>python -m py_compile main.py</automated>
  </verify>
  <done>Imports organized into 3 clear categories with section markers</done>
</task>

<task type="auto">
  <name>Task 2: Add major section markers to launch_gui()</name>
  <files>main.py</files>
  <read_first>
    - main.py (lines 233-2516 contain launch_gui() function)
    - .planning/phases/03-split-main-py-further-higher-risk/03-RESEARCH.md (lines 184-236 show recommended structure)
  </read_first>
  <action>
Add major section markers throughout launch_gui() function to improve navigation. Use the double-line style (═══) for major sections.

**Insert these section markers at the following locations:**

1. **After line 234 (start of launch_gui):**
```python
def launch_gui():
    """Launch main GUI application."""
    # ═══════════════════════════════════════════════════════════
    # SECTION 1: Window Setup
    # ═══════════════════════════════════════════════════════════
    root = tk.Tk()
    ...
```

2. **Before line 246 (config loading):**
```python
    # ═══════════════════════════════════════════════════════════
    # SECTION 2: Configuration & State Management
    # ═══════════════════════════════════════════════════════════
    # Load saved config
    app_config = load_app_config()
    ...
```

3. **Before the notebook/tabs creation (around line 300):**
```python
    # ═══════════════════════════════════════════════════════════
    # SECTION 3: UI Layout - Notebook & Tabs
    # ═══════════════════════════════════════════════════════════
    notebook = ttk.Notebook(...)
    ...
```

4. **Before source tab creation (around line 400):**
```python
    # ═══════════════════════════════════════════════════════════
    # SECTION 4: Source Tab - Video Input & Processing
    # ═══════════════════════════════════════════════════════════
    source_tab = tk.Frame(...)
    ...
```

5. **Before adjust/subtitle tab creation (around line 900):**
```python
    # ═══════════════════════════════════════════════════════════
    # SECTION 5: Adjust Tab - Subtitle Parameters
    # ═══════════════════════════════════════════════════════════
    adjust_tab = tk.Frame(...)
    ...
```

6. **Before dub/TTS tab creation (around line 1700):**
```python
    # ═══════════════════════════════════════════════════════════
    # SECTION 6: Dub Tab - TTS Configuration
    # ═══════════════════════════════════════════════════════════
    dub_tab = tk.Frame(...)
    ...
```

7. **Before log tab creation (around line 2000):**
```python
    # ═══════════════════════════════════════════════════════════
    # SECTION 7: Log Tab - Output & Status
    # ═══════════════════════════════════════════════════════════
    log_tab = tk.Frame(...)
    ...
```

8. **Before controller initialization (around line 2400):**
```python
    # ═══════════════════════════════════════════════════════════
    # SECTION 8: Controller Initialization & Event Wiring
    # ═══════════════════════════════════════════════════════════
    source_ctrl = SourceController(...)
    ...
```

9. **Before root.mainloop() (around line 2510):**
```python
    # ═══════════════════════════════════════════════════════════
    # SECTION 9: Start Application
    # ═══════════════════════════════════════════════════════════
    root.mainloop()
```

**Keep existing subsection markers** (single-line ───) for smaller sections within tabs.

**Line count impact:** Adding ~27 lines (9 sections × 3 lines each), but improves navigation significantly.
  </action>
  <acceptance_criteria>
    - main.py contains exactly 9 major section markers with "# ═══" style
    - Section 1 marker appears after "def launch_gui():" line
    - Section 2 marker appears before "app_config = load_app_config()"
    - Section 3 marker appears before notebook creation
    - Sections 4-7 mark each tab creation (Source, Adjust, Dub, Log)
    - Section 8 marker appears before controller initialization
    - Section 9 marker appears before "root.mainloop()"
    - grep "# ═══" main.py | wc -l returns 27 (9 sections × 3 lines)
    - Existing subsection markers (# ──) remain unchanged
  </acceptance_criteria>
  <verify>
    <automated>python -m py_compile main.py && grep -c "# ═══" main.py</automated>
  </verify>
  <done>9 major section markers added to launch_gui() for clear navigation</done>
</task>

<task type="auto">
  <name>Task 3: Verify organization and document changes</name>
  <files>main.py</files>
  <read_first>
    - main.py (full file to verify all changes)
  </read_first>
  <action>
Verify the reorganization is complete and document the improvements:

1. **Syntax check:**
   - python -m py_compile main.py

2. **Import verification:**
   - Verify all imports still work: python -c "import main; print('OK')"
   - Check no imports were lost: count should match original

3. **Section marker verification:**
   - Count major sections: grep "# ═══" main.py | wc -l (should be 27)
   - Count subsections: grep "# ──" main.py | wc -l (should be ~15-20)

4. **Line count check:**
   - wc -l main.py (should be ~2,517 lines: 2,490 from Plan 01 + 27 new section markers)

5. **Navigation test:**
   - Verify sections are easy to find: grep -n "SECTION [0-9]:" main.py
   - Should show 9 clear section headers with line numbers

6. **Document improvements:**
   - Imports organized into 3 categories
   - 9 major section markers added
   - Code structure significantly improved for navigation
   - No functionality changes (pure organization)
  </action>
  <acceptance_criteria>
    - python -m py_compile main.py exits 0
    - python -c "import main; print('OK')" succeeds
    - grep "# ═══" main.py | wc -l returns 27
    - grep -n "SECTION [0-9]:" main.py returns 9 matches
    - main.py line count is approximately 2,517 lines
    - All original imports present (no imports lost)
  </acceptance_criteria>
  <verify>
    <automated>python -m py_compile main.py && grep -c "SECTION [0-9]:" main.py</automated>
  </verify>
  <done>Organization verified: imports categorized, 9 section markers added, navigation improved</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Code organization only - no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | main.py imports | accept | Import organization does not change security - all imports already validated |

No new security concerns - reorganizing code does not change security posture.
</threat_model>

<verification>
After all tasks complete:

1. **Syntax Check**: main.py compiles without errors
2. **Import Check**: All imports work correctly
3. **Section Check**: 9 major sections clearly marked
4. **Navigation Check**: Easy to find sections with grep
5. **No Regressions**: All functionality preserved
</verification>

<success_criteria>
- ✅ Imports organized into 3 categories (stdlib, third-party, local)
- ✅ Category markers added (# ── Standard Library ──, etc.)
- ✅ 9 major section markers added to launch_gui()
- ✅ Section markers use consistent style (═══ for major, ─── for subsections)
- ✅ main.py compiles without errors
- ✅ All imports still work
- ✅ Code navigation significantly improved
- ✅ No functionality changes
</success_criteria>

<output>
After completion, create `.planning/phases/03-split-main-py-further-higher-risk/03-02-SUMMARY.md`
</output>
