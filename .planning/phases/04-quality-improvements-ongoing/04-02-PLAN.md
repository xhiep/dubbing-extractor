---
phase: 04-quality-improvements-ongoing
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/modules/workflow.py
  - src/modules/transcription/whisper_engine.py
  - src/modules/transcription/translator.py
  - src/modules/transcription/srt_generator.py
  - src/modules/downloader/ytdlp_wrapper.py
  - src/modules/video_processing/ffmpeg_wrapper.py
  - src/modules/video_processing/subtitle_detector.py
  - src/modules/video_processing/video_encoder.py
  - src/modules/video_processing/subtitle_burner.py
  - src/modules/tts/vieneu_engine.py
  - src/modules/tts/audio_dubber.py
  - src/controllers/app_controller.py
  - src/controllers/source_controller.py
  - src/controllers/subtitle_controller.py
  - src/controllers/tts_controller.py
autonomous: true
requirements: [QI-03]

must_haves:
  truths:
    - "Public functions in workflow.py have type hints"
    - "Public functions in all module files have type hints"
    - "Controller methods have type hints"
    - "mypy runs without errors on annotated files"
  artifacts:
    - path: "src/modules/workflow.py"
      provides: "Type hints on process_video() and public functions"
      contains: "def process_video("
    - path: "mypy.ini"
      provides: "mypy configuration for incremental adoption"
      min_lines: 10
  key_links:
    - from: "mypy.ini"
      to: "src/modules/"
      via: "configuration enables type checking"
      pattern: "\\[mypy-src\\.modules\\.\\*\\]"

---

<objective>
Add type hints to all public APIs across modules and controllers.

Purpose: Enable static type checking with mypy to catch type errors before runtime and improve IDE autocomplete/documentation.

Output: Type hints on all public functions (not prefixed with _), mypy configuration file, type checking passes on public APIs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-quality-improvements-ongoing/04-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mypy configuration</name>
  <files>mypy.ini</files>
  <action>
Create mypy.ini in project root with incremental adoption configuration:

```ini
[mypy]
python_version = 3.11
warn_return_any = True
warn_unused_configs = True
ignore_missing_imports = True

# Only enforce type checking on our code, not third-party libraries
[mypy-src.modules.*]
disallow_untyped_defs = True

[mypy-src.controllers.*]
disallow_untyped_defs = True
```

This follows the pattern from 04-RESEARCH.md "Pitfall 5: Mypy Configuration Too Strict" - start with basic checks, ignore third-party libraries without stubs.
  </action>
  <verify>
test -f mypy.ini && grep -q "python_version = 3.11" mypy.ini && grep -q "ignore_missing_imports = True" mypy.ini
  </verify>
  <done>mypy.ini exists with incremental adoption configuration</done>
</task>

<task type="auto">
  <name>Task 2: Add type hints to workflow.py public functions</name>
  <files>src/modules/workflow.py</files>
  <action>
Add type hints to all public functions in workflow.py. Import types from typing and pathlib:

```python
from pathlib import Path
from typing import Optional, Callable, Dict, List
```

**Functions to annotate:**

1. `process_video()` - main pipeline function:
```python
def process_video(
    source_input: str,
    cover_mode: str = "blur",
    dub_mode: str = "none",
    log_cb: Optional[Callable[[str], None]] = None
) -> Dict[str, Path]:
```

2. Any other public functions (not prefixed with _) - add appropriate type hints based on their parameters and return values.

**Private functions (_prefixed):** Skip type hints per 04-RESEARCH.md "Pitfall 2: Type Hints on Private Functions".

Follow pattern from 04-RESEARCH.md "Pattern 3: Type Hints for Public APIs".
  </action>
  <verify>
python -m py_compile src/modules/workflow.py && grep -q "def process_video(" src/modules/workflow.py && grep -q "-> Dict\[str, Path\]:" src/modules/workflow.py
  </verify>
  <done>workflow.py public functions have complete type hints</done>
</task>

<task type="auto">
  <name>Task 3: Add type hints to all other module public functions</name>
  <files>src/modules/transcription/whisper_engine.py, src/modules/transcription/translator.py, src/modules/transcription/srt_generator.py, src/modules/downloader/ytdlp_wrapper.py, src/modules/video_processing/ffmpeg_wrapper.py, src/modules/video_processing/subtitle_detector.py, src/modules/video_processing/video_encoder.py, src/modules/video_processing/subtitle_burner.py, src/modules/tts/vieneu_engine.py, src/modules/tts/audio_dubber.py, src/controllers/app_controller.py, src/controllers/source_controller.py, src/controllers/subtitle_controller.py, src/controllers/tts_controller.py  <action>
Add type hints to all public functions (not prefixed with _) in the following modules:

**Import types at top of each file:**
```python
from pathlib import Path
from typing import Optional, Callable, Dict, List, Tuple, Any
```

**For each module, annotate public functions:**

- **transcription/whisper_engine.py**: transcribe() function
  - Parameters: audio: Path, log_cb: Optional[Callable[[str], None]] = None
  - Return: List[Dict[str, Any]]

- **transcription/translator.py**: translate() function
  - Parameters: text: str, source_lang: str = "en", target_lang: str = "vi"
  - Return: str

- **transcription/srt_generator.py**: write_srt(), parse_srt() functions
  - write_srt: segments: List[Dict], output_path: Path -> None
  - parse_srt: srt_path: Path -> List[Dict]

- **downloader/ytdlp_wrapper.py**: download() function
  - Parameters: url: str, output_dir: Path, log_cb: Optional[Callable[[str], None]] = None
  - Return: Path

- **video_processing/ffmpeg_wrapper.py**: extract_audio_local(), get_dims() functions
  - extract_audio_local: video_path: Path, output_path: Path -> None
  - get_dims: video_path: Path -> Tuple[int, int]

- **video_processing/subtitle_detector.py**: detect_sub_events() function
  - Parameters: video_path: Path, log_cb: Optional[Callable[[str], None]] = None
  - Return: List[Dict[str, Any]]

- **video_processing/video_encoder.py**: render_clean_video() function
  - Parameters: video_path: Path, output_path: Path, cover_mode: str, events: List[Dict], log_cb: Optional[Callable[[str], None]] = None
  - Return: None

- **video_processing/subtitle_burner.py**: burn_subtitle() function
  - Parameters: video_path: Path, srt_path: Path, output_path: Path, log_cb: Optional[Callable[[str], None]] = None
  - Return: None

- **tts/vieneu_engine.py**: Public TTS functions (check file for actual function names)
  - Add appropriate type hints based on function signatures

- **tts/audio_dubber.py**: render_dubbed_outputs() function
  - Parameters: video_path: Path, segments: List[Dict], output_dir: Path, log_cb: Optional[Callable[[str], None]] = None
  - Return: Dict[str, Path]

- **controllers/*.py**: All controller class methods
  - __init__ methods: self, widgets: Any, config: Dict
  - Event handler methods: self -> None (most handlers return None)

**Rules:**
- Only annotate public functions (not prefixed with _)
- Use Path from pathlib for file paths
- Use Optional[Callable[[str], None]] for log callbacks
- Use List[Dict] or List[Dict[str, Any]] for segment lists
- Use Dict[str, Path] for output path dictionaries
- Skip private helper functions
  </action>
  <verify>
# Syntax check all modified files
python -m py_compile src/modules/transcription/whisper_engine.py src/modules/transcription/translator.py src/modules/transcription/srt_generator.py src/modules/downloader/ytdlp_wrapper.py src/modules/video_processing/ffmpeg_wrapper.py src/modules/video_processing/subtitle_detector.py src/modules/video_processing/video_encoder.py src/modules/video_processing/subtitle_burner.py src/modules/tts/vieneu_engine.py src/modules/tts/audio_dubber.py src/controllers/app_controller.py src/controllers/source_controller.py src/controllers/subtitle_controller.py src/controllers/tts_controller.py

# Verify typing imports added
grep -l "from typing import" src/modules/transcription/whisper_engine.py src/modules/transcription/translator.py src/modules/transcription/srt_generator.py src/modules/downloader/ytdlp_wrapper.py src/modules/video_processing/ffmpeg_wrapper.py src/modules/video_processing/subtitle_detector.py src/modules/video_processing/video_encoder.py src/modules/video_processing/subtitle_burner.py src/modules/tts/vieneu_engine.py src/modules/tts/audio_dubber.py | wc -l

# Run mypy on modules (expect some errors, but should not crash)
pip install mypy 2>/dev/null || echo "mypy not installed, skipping type check"
mypy src/modules/workflow.py 2>&1 | head -20 || echo "mypy check attempted"
  </verify>
  <done>All public functions in modules and controllers have type hints, mypy configuration in place</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Type hints → Runtime | Type hints are development-time only, no runtime validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | None | type hints | accept | Type hints are static analysis only, no security impact at runtime |
</threat_model>

<verification>
After all tasks complete:

1. Run syntax check on all modified files
2. Install mypy: pip install mypy
3. Run mypy on workflow.py: mypy src/modules/workflow.py
4. Check that type hints are present in public functions
5. Verify application still runs: scripts\run.bat
</verification>

<success_criteria>
- mypy.ini exists with incremental adoption configuration
- workflow.py has type hints on process_video() and other public functions
- All module public functions have type hints
- All controller methods have type hints
- Application runs without errors
- Type hints follow Python 3.11 syntax
</success_criteria>

<output>
After completion, create `.planning/phases/04-quality-improvements-ongoing/04-02-SUMMARY.md`
</output>
